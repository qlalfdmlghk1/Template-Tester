# company-research-enhancement — 진행 상황

## 📌 현재 작업

- 이슈: [#81](https://github.com/qlalfdmlghk1/Template-Tester/issues/81) (Feat)
- 브랜치: `feature/81-company-research-enhancement`
- 단계: **구현 완료, 손 검증 대기 중 (PR 미생성)**
- 마지막 업데이트: 2026-08-09

### ▶️ 이어서 할 때 — 무료로 바로 검증 가능

AI 제공자를 고를 수 있다. **Gemini는 무료 사용량이 있어 결제 없이 검증할 수 있다.**

1. [aistudio.google.com/apikey](https://aistudio.google.com/apikey)에서 Gemini 키 발급 (무료, 카드 불필요)
2. 앱에서 기업 수정 → "AI로 조사" 옆 **키 설정하기** → 제공자 `Gemini` 선택 후 키 입력
3. 조사 실행 → **결과가 사실과 맞는지 출처 링크로 대조**
4. 그 결과를 보고 남은 ⚠️ 5건(#10·#11·#13·#16·#17) 판단
5. `/pr` 로 PR 생성

Claude를 쓰려면 [console.anthropic.com](https://console.anthropic.com) → Plans & Billing에서 크레딧 충전이 필요하다.
**Claude 구독(Pro/Max)과 API 크레딧은 별개 결제**라 구독만으로는 API가 열리지 않는다 — 실제로 이 때문에 한 번 막혔다.

모델명(`gemini-2.5-flash`)이 맞지 않으면 404와 사유가 화면에 그대로 나온다. 그 경우
`entities/company/api/research.gemini.ts`의 `MODEL` 상수만 조정하면 된다.

---

## [Issue #81] 기업 조사 페이지 고도화

**Type**: Feat | **Jira**: 미사용 | **시작**: 2026-08-08

### ✅ 완료

- [x] 작업 환경 셋업 (/start 실행)
- [x] 기획 검수 (/plan-review) — 3라운드, 🚫 3건 도출 후 전부 해소
- [x] **브라우저 직접 호출 CORS 검증** (⚠️ #25) — 헤더 필요, 웹 검색 포함도 통과
- [x] `Company` 스키마 확장 — 인재상·사업 내용·최근 이슈 + 출처·조사 시각
- [x] 채움률 순수 함수 (`entities/company/model/research.ts`) + 테스트 7건
- [x] API 키 스토어 (`shared/lib/useAiKey.ts`) + 테스트 10건
- [x] AI 조사 호출 (`entities/company/api/research.api.ts`) + 테스트 13건

- [x] AI 조사 UI — 키 입력 화면, 조사 트리거, 결과 미리보기(필드별 반영 선택), 채움률 표시

### 🚧 진행 중

- [ ] 실제 키로 손 검증 (조사 품질·출처 정확성)

### 📝 결정 로그

- [2026-08-08] /start 실행, 작업 환경 셋업 완료
- [2026-08-08] **AI 호출 경로 = 사용자 본인 키 + 클라이언트 직접 호출** (두 번 뒤집힌 결정)
  - 처음엔 Functions 프록시 재사용으로 정했으나, Functions 배포에 Blaze(카드 등록)가 필수라는 제약 때문에 뒤집음
  - 대안 3가지(무료 프록시 이전 / 사용자 본인 키 / AI 조사 보류) 중, **사용 범위가 본인 + 지인 소수**여서 사용자 본인 키를 선택
  - 근거: 우리 키가 아니라 사용자 키라 유출 시 피해가 본인 크레딧으로 한정. 레포에 `dangerouslySetInnerHTML` 0건, 런타임 의존성 11개로 XSS·공급망 표면이 작음
  - 남는 위험은 공급망(FE 의존성 오염 시 키 탈취) → **FE에 Anthropic SDK를 추가하지 않고 `fetch`로 호출**해 표면을 더 줄임
  - 재검토 조건: 불특정 다수가 쓰는 서비스가 되면 무료 프록시(Cloudflare Workers 등)로 전환
- [2026-08-08] **Daily 학습도 사용자 키 방식으로 이전 결정** → 후속 이슈 [#82](https://github.com/qlalfdmlghk1/Template-Tester/issues/82). `functions/` 제거까지 포함하며, 완료되면 프로젝트에서 Blaze(카드) 요구가 완전히 사라지고 Windows 에뮬레이터 기동 문제도 해소됨. **#81의 CORS 검증 통과 후 착수** (막힐 경우 대비해 Functions를 먼저 지우지 않음)
- [2026-08-08] **공고 마감일 기능 폐기**. 마감일의 소속 엔티티(`Company` vs `JobApplication`)가 미결 — `Company`는 재지원해도 1건만 유지되는데 마감일은 공고마다 다름. 잘못 정하면 되돌리는 비용이 커서 별도 이슈로 분리
- [2026-08-08] **AI 조사 = 웹 검색 근거**. "최근 이슈"는 모델 파라메트릭 지식만으로는 원리적으로 채울 수 없어(학습 컷오프) 서버사이드 웹 검색 도구를 사용하기로 함. 각 항목에 출처 URL을 함께 저장
- [2026-08-08] **자소서 문항 메모는 기획서 3.4 "자기소개서 관리"로 이관**. 중복 기획이라 이번 범위에서 제외. 3.4가 미구현이라 지금은 연결할 대상이 없어 연결 지점만 열어둠

### 🐛 트러블슈팅

<!-- /note troubleshoot 으로 추가 -->

### ⏭️ 남은 작업

- [ ] 실제 API 키로 조사 실행해 결과 품질·출처 확인 (수동)
- [ ] Storybook 스토리 (`shared/ui` 변경은 없으나 신규 UI 문서화 검토)

### ⚠️ 남은 확인 항목 (UI 착수 시 결정)

`planning-review.md` 기준 잔여 8건 중 UI에 직접 걸리는 것들:

- **#10 실패 시 처리**: 자동 재시도를 할지, 부분 결과(3개 중 일부만 채워짐)를 보여줄지
- **#11 자유 메모와의 관계**: 기존 `researchNote`에 적힌 인재상 등을 옮기도록 안내할지
- **#13 동명 기업**: 단서(직무·위치·공고 URL) 전달은 구현했으나, 결과가 엉뚱한 기업일 때의 UX 미정
- **#16 검색 대상**: 신규 필드를 키워드 검색 대상에 넣을지
- **#17 조사 중 이탈**: 다이얼로그를 닫거나 페이지를 벗어나면 취소할지 유지할지

해소된 항목: #4(필드별 선택) · #6(분모 7개) · #7(판정 기준 유지) · #25(CORS 통과) · #26(비활성화 + 안내) · #27(→ #82)

### 🔒 보안 수칙 (이 방식의 전제 — 구현 내내 유지)

- 키를 **Firestore에 저장하지 않는다** (기기 간 동기화 유혹 주의)
- **AI 응답을 HTML로 렌더링하지 않는다** — `dangerouslySetInnerHTML` 0건 유지
- 키를 콘솔·로그에 출력하지 않는다, 표시는 마스킹
- FE에 Anthropic SDK를 추가하지 않는다 (`fetch` 사용)

---

### Commit — 2026-08-08 17:32

- Message: `Docs:#81 기업 조사 고도화 기획 문서 추가`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `plan.md` — 작업 목적·범위·접근 방식·검증 계획. 3라운드 기획 검수와 사용자 결정 4건이 반영된 상태
- `planning-review.md` — 기획 검수 결과. 초기 🚫 3건 전부 해소, 현재 ⚠️ 10건 / 💡 4건
- `progress.md` — 진행 상황·결정 로그·보안 수칙

**결정 로그**

- AI 호출 경로가 두 번 뒤집힘: 클라이언트 직접 → Functions 프록시 → **사용자 본인 키 + 클라이언트 직접**. Functions 배포에 Blaze(카드 등록)가 필요하다는 제약이 결정적이었음
- 웹 검색 근거 조사로 확정 (모델 지식만으로는 "최근 이슈"를 채울 수 없음). 모델은 `claude-sonnet-5`
- 공고 마감일·자소서 문항 메모는 Out of Scope로 분리
- Daily 학습도 같은 키 방식으로 이전하기로 하고 후속 이슈 #82 생성

**다음 작업**

- ⚠️ #25 검증: 브라우저에서 Anthropic API 직접 호출이 CORS로 막히지 않는지 확인. 막히면 접근 방식을 무료 프록시로 재설계해야 하므로 코드 작성보다 먼저 확인

---

### Commit — 2026-08-08 17:55

- Message: `Feat:#81 기업 조사 신규 필드와 AI 조사 호출 계층 추가`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `company.type.ts` — 인재상·사업 내용·최근 이슈 필드 추가. 전부 optional이라 기존 Firestore 문서 마이그레이션 불필요. 출처(`researchSources`)와 조사 시각(`researchedAt`)도 함께 보관
- `company.api.ts` — Firestore 매핑에 신규 필드 반영
- `research.ts` (신규) — 채움률 계산 순수 함수. 분모는 조사 항목 7개
- `useAiKey.ts` (신규) — 사용자 Anthropic 키 보관. `useTheme` 패턴(훅 + localStorage). Daily도 쓸 수 있게 shared에 배치
- `research.api.ts` (신규) — 웹 검색 기반 AI 조사 호출. `fetch` 직접 사용, `pause_turn` 재개 루프, 에러 종류 구분
- 테스트 30건 추가 (채움률 7 / 키 스토어 10 / AI 호출 13)

**결정 로그**

- **CORS 검증 완료**: `anthropic-dangerous-direct-browser-access: true` 헤더가 있어야 브라우저 직접 호출이 통과한다. 헤더 없이는 preflight에서 차단. 웹 검색 도구를 포함한 요청도 동일하게 통과. Chromium(Playwright)에서 실측 — `curl`은 CORS를 강제하지 않아 검증 수단이 아니다
- **채움률 분모 = 조사 항목 7개**, 기본 정보는 제외
- **`hasResearch()`는 기존 4개 필드 기준 유지** — 신규 필드를 넣으면 기존 기업이 갑자기 미완료로 바뀌어 목록 카운트·필터가 흔들린다. 이 의도를 테스트로 고정해 뒀다
- **키 저장은 Zustand가 아니라 훅 + localStorage** — 컨벤션 문서는 Zustand를 말하지만 `package.json`에 없다. 기존 `useTheme` 패턴을 따랐다
- **Anthropic SDK 미도입** — 키가 브라우저에 있는 구조에서 남는 위험이 공급망이라 런타임 의존성을 늘리지 않았다

**다음 작업**

- AI 조사 UI: 키 입력 화면, 조사 트리거 버튼(키 없으면 비활성 + 안내), 결과 미리보기에서 필드별 반영 선택
- UI 착수 시 남은 ⚠️: #10(실패 시 재시도 정책), #11(자유 메모와의 관계 안내), #13(동명 기업 — 단서 전달은 구현했으나 UX 미정), #16(검색 대상에 신규 필드 포함 여부), #17(조사 중 이탈)

---

### Commit — 2026-08-08 18:18

- Message: `Feat:#81 AI 조사 UI와 채움률 표시 추가`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `features/ai-key/ui/AiKeyDialog` (신규) — 키 입력·삭제. 마스킹 표시, 발급 링크, 지출 한도 안내. Daily도 쓸 수 있게 별도 슬라이스로 분리
- `useCompanyAiResearch.ts` (신규) — 조사 실행·선택 상태. 에러는 사용자용 문구로만 노출하고 원본은 콘솔에 찍지 않는다(키 유출 방지)
- `AiResearchPreview` (신규) — 항목별 체크박스, 기존 값이 있으면 "덮어씀" 표시, 출처 링크(`safeUrl` 통과분만)
- `CompanyResearchDialog` — 인재상·사업 내용·최근 이슈 입력란 + AI 조사 버튼. 키 없으면 비활성 + 설정 링크
- `CompanyResearchCard` — 채움률 막대(수치 병기) + 신규 항목 섹션
- `pages/companies/research.tsx` — 키 설정 다이얼로그 연결
- 테스트 7건 추가 (누적 414건 전체 통과)

**결정 로그**

- **`surfaceAlt` 토큰은 존재하지 않았다** — 처음 작성 시 임의로 썼다가 `tailwind.config.js`에 없음을 확인하고 `bg-background`로 교체. 없는 토큰은 조용히 무시돼 배경이 안 나온다
- 채움률은 **색 + 수치(N/7)** 로 표시 — 색만으로 구분하면 색각 이상 사용자가 읽을 수 없다
- AI 결과·출처 모두 **텍스트로만 렌더링**. 출처 URL은 `safeUrl`로 스킴을 거른 것만 링크로 만든다
- 조사 결과는 폼 상태에만 반영되고, 저장은 기존 "저장" 버튼을 눌러야 일어난다 — 실수로 눌러도 되돌릴 수 있게

**다음 작업**

- 실제 API 키로 조사 실행해 결과 품질·출처 정확성 확인 (수동)
- 남은 ⚠️ 5건(#10·#11·#13·#16·#17)은 손 검증 후 판단하는 편이 정확

---

### Commit — 2026-08-08 18:50

- Message: `Fix:#81 AI 조사 실패 원인이 화면에 드러나지 않던 문제 수정`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `research.api.ts` — 실패 응답 본문의 `error.message`를 읽어 메시지에 포함. 크레딧 부족은 `credit` 종류로 구분해 한국어 안내로 치환
- `AiKeyDialog` — 키 발급만으로는 동작하지 않고 크레딧 충전이 필요하다는 안내 추가
- 테스트 3건 추가 (크레딧 부족 / 400 본문 노출 / 본문 파싱 실패 시 상태 코드 유지)

**결정 로그**

- **실패 응답 본문을 버리던 것이 결함이었다.** 처음엔 `HTTP 400`만 표시해 원인을 알 수 없었고, 실제 원인(크레딧 잔액 부족)을 찾는 데 사용자가 개발자도구를 뒤져야 했다. Anthropic은 400 본문에 원인을 명시하므로 이를 그대로 노출한다
- 크레딧 부족은 **키 발급 후 충전을 안 한 상태**로, 이 방식(사용자 본인 키)에서 가장 흔한 실패다. 영어 원문 대신 무엇을 해야 하는지 알려주는 문구로 바꿨다
- 429 문구에서 "크레딧 부족"을 뺐다 — 크레딧 문제는 실제로 400으로 온다

**다음 작업**

- 크레딧 충전 후 실제 조사 실행해 결과 품질·출처 확인 (수동)

---

### Commit — 2026-08-09 01:50

- Message: `Feat:#81 AI 제공자 선택(Gemini·Claude) 추가`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `research.shared.ts` (신규) — 제공자 무관 공통부. 프롬프트·파싱·에러 타입을 한곳에 모아 제공자가 달라도 결과 형식이 흔들리지 않게 함
- `research.gemini.ts` (신규) — Google 검색 grounding 기반 조사. 무료 사용량으로 동작
- `research.anthropic.ts` (신규) — 기존 Claude 경로를 분리
- `research.api.ts` — 제공자 분기 진입점으로 축소
- `useAiKey.ts` — 제공자 선택 + 제공자별 키 보관. 기존 `anthropic-api-key` 저장소 이름은 유지해 이미 키를 넣은 사용자가 다시 입력하지 않게 함
- `AiKeyDialog` — 제공자 선택 UI, Claude 선택 시 크레딧 필요 안내, 제공자별 발급 링크
- 테스트 24건 추가 (누적 438건 전체 통과)

**결정 로그**

- **Gemini 브라우저 직접 호출 가능 확인** — Chromium 실측. Anthropic 과 달리 별도 허용 헤더가 필요 없고, `google_search` 도구를 포함한 요청도 통과. 코드 작성 전에 확인해 헛수고를 막았다
- **기본 제공자는 Gemini** — 새 사용자가 크레딧 충전 없이 바로 쓸 수 있어야 한다. 단, 이미 Anthropic 키를 넣어둔 사용자는 그 선택을 유지
- **Gemini 키는 URL 이 아니라 `x-goog-api-key` 헤더로 전송** — URL 쿼리에 넣으면 브라우저 기록·리퍼러에 남는다
- **JSON 응답 모드 대신 프롬프트로 JSON 요구** — 검색 grounding 과 JSON 모드를 함께 쓸 수 없어, 응답 텍스트에서 직접 파싱한다. 모델이 앞뒤에 설명을 붙이는 경우를 대비해 첫 `{`~마지막 `}` 재시도 로직을 넣음
- **Gemini 출처 보완** — 모델이 sources 를 비워 보내면 grounding 이 실제 참조한 URL 로 메운다
- 테스트 실행 시 병렬 워커 기동이 타임아웃되는 환경 문제가 있어 `--no-file-parallelism` 으로 확인

**다음 작업**

- Gemini 키로 실제 조사 실행해 결과 품질·출처 확인 (수동). 무료라 크레딧 충전 없이 가능
- 모델명(`gemini-2.5-flash`)이 맞지 않으면 404 와 사유가 화면에 그대로 나오므로, 그 경우 상수만 조정

---

### Commit — 2026-08-09 02:10

- Message: `Feat:#81 Gemini 사용량 한도를 종류별로 구분해 안내`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `research.shared.ts` — `readErrorMessage` → `readErrorBody`. 메시지뿐 아니라 본문 원문도 돌려줘 세부 사유를 찾을 수 있게 함
- `research.gemini.ts` — 하루 한도 / 분당 한도 구분. 분당이면 `retryDelay` 를 읽어 대기 시간까지 안내
- 테스트 3건 추가 (누적 441건 전체 통과)

**결정 로그**

- **한도 종류를 구분해야 하는 이유**: 분당 한도는 기다리면 풀리지만 하루 한도는 오늘 안에 안 풀린다. 같은 문구로 뭉뚱그리면 사용자가 무의미하게 기다리거나, 반대로 금방 될 걸 포기한다. 하루 한도일 때는 제공자 전환을 안내한다
- 사유가 `error.message` 가 아니라 `details[].violations[].quotaId` 에 담겨 오는 경우가 있어, **본문 전체 문자열에서 단서를 찾는다**. 필드 위치를 특정하면 응답 형태가 조금만 달라져도 놓친다
- 구글이 문서에서 무료 한도 수치를 뺐다 — 계정별로 다를 수 있어 `aistudio.google.com/rate-limit` 에서 확인해야 한다

**다음 작업**

- 남은 ⚠️ 5건(#10·#11·#13·#16·#17) 판단 후 `/pr`

---

### Commit — 2026-08-09 02:40

- Message: `Refactor:#81 기업 등록·수정을 모달에서 페이지로 전환`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `pages/companies/research.tsx` → `research/index.tsx` (목록), `research/new.tsx`·`research/[id].tsx` 추가
- `CompanyResearchDialog` → `CompanyResearchForm` (오버레이 제거). 페이지 껍데기는 `CompanyResearchFormPage` 로 분리해 등록·수정이 공유
- `useCompanyForm` (신규) — 폼 전용. 대상 조회·저장·목록 복귀
- `useCompanyResearch` — 모달 상태 제거. **검색어·필터를 URL 쿼리로 이전**
- 페이지 상단에 "← 목록으로" 버튼 추가

**결정 로그**

- **모달 → 페이지 전환 이유**: AI 조사 미리보기가 들어가면서 폼이 모달로 감당하기 어려워졌다. 좁은 창 안에서 조사 결과를 보며 아래 입력란을 채우려면 스크롤이 반복된다
- **필터를 URL 쿼리로 올린 이유**: 모달일 때는 뒤에 목록이 살아 있어 필터가 유지됐다. 페이지로 바꾸면 돌아왔을 때 초기화돼 **후퇴가 된다**. 쿼리로 올려 유지하고, 덤으로 필터 걸린 목록을 링크로 공유할 수 있게 했다
- 폼 페이지도 쿼리를 물고 다니게 해 `취소`·`뒤로가기`가 필터를 그대로 들고 돌아간다
- 라우트는 `daily/index.tsx` + `daily/[id].tsx` 기존 패턴과 동일하게 뒀다
- 지원 현황의 지원 건 등록은 계속 모달이다 — 조사 폼은 길고 지원 건 폼은 짧아 성격이 다르다. 통일이 필요해지면 별도 이슈

**남은 고려**

- 폼 작성 중 이탈 시 경고가 없다(⚠️ #17 과 연결). 모달일 때도 없던 문제지만, 페이지가 되면서 브라우저 뒤로가기 경로가 늘었다
- 존재하지 않는 경로는 빈 화면이 뜬다 — 기존부터 있던 문제로 이번 범위 밖

**다음 작업**

- 남은 ⚠️ 5건(#10·#11·#13·#16·#17) 판단 후 `/pr`

---

### Commit — 2026-08-09 03:05

- Message: `Refactor:#81 기업 등록·수정 폼을 2열 레이아웃으로 정리`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `CompanyResearchForm` — 한 열 → **2열**. 좌측(380px 고정)에 기본 정보·직무 설명·자격 요건·메모, 우측(가변)에 기업 조사 항목
- 조사 항목 입력란을 크게 키움 — 인재상 8행, 사업 내용 10행, 최근 이슈 12행 (기존 일괄 3행)
- 저장·취소를 `sticky bottom-0` 으로 고정
- `CompanyResearchFormPage` — 폭 900px → **1400px** (목록·코테달력과 동일)

**결정 로그**

- **AI 조사 결과가 3행 입력란에 들어가니 읽을 수가 없었다.** 실제 조사 결과는 문단 여러 개 분량이라 스크롤 없이는 확인이 불가능했다
- 좌우 배치는 코테달력의 `lg:grid-cols-[1fr_360px]` 패턴을 **좌우 반전**해 적용 — 조사 내용이 주인공이므로 넓은 쪽을 오른쪽에 줬다
- 좁은 화면(`lg` 미만)에서는 한 열로 쌓인다
- 폼이 길어져 저장 버튼이 화면 밖으로 밀리므로 하단 고정

**다음 작업**

- 남은 ⚠️ 5건(#10·#11·#13·#16·#17) 판단 후 `/pr`

---

### Commit — 2026-08-09 03:25

- Message: `Feat:#81 조사 출처를 폼·카드에도 표시`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `ResearchSourceLinks` (신규) — 출처 링크 공용 컴포넌트. `AiResearchPreview` 안에 있던 구현을 끌어내 재사용
- `CompanyResearchForm` — 인재상·사업 내용·최근 이슈 입력란 아래에 저장된 출처 노출
- `CompanyResearchCard` — 각 조사 섹션 아래에 출처 노출
- 링크 라벨을 `[1]`·`[2]` → **도메인**(`samsunglife.com`)으로 변경
- 테스트 5건 추가 (누적 446건 전체 통과)

**결정 로그**

- **출처가 저장은 되는데 보이지 않던 것이 누락이었다.** `researchSources` 를 Firestore 에 넣고 있었지만 미리보기에서만 그렸고, 반영·저장한 뒤에는 어디서도 확인할 수 없었다. AI 결과는 초안이라 나중에도 근거를 되짚을 수 있어야 한다
- **번호 대신 도메인 표시** — `[1]` 만으로는 어디로 가는 링크인지 알 수 없어 확인 동선이 끊긴다
- 출처 URL 도 `safeUrl` 로 스킴을 거른 것만 링크로 만든다 (허용 스킴 외는 렌더 자체를 하지 않음). 이 동작을 테스트로 고정

**다음 작업**

- 남은 ⚠️ 5건(#10·#11·#13·#16·#17) 판단 후 `/pr`

---

### Commit — 2026-08-09 04:00

- Message: `Fix:#81 출처 링크가 모두 같은 주소로 보이던 문제 수정`
- Issue: `#81`
- Jira: 미사용

**변경 요약**

- `ResearchSource` 타입 신설 — 출처를 `string` 에서 `{ url, title }` 로 변경
- `research.gemini.ts` — grounding chunk 의 `title` 을 함께 챙기고, 항목당 fallback 출처를 4개로 제한
- `ResearchSourceLinks` — 제목 우선, 없으면 도메인, 리다이렉트 호스트면 `출처 N`
- `company.api.ts` — 기존에 저장된 문자열 배열을 읽는 쪽에서 흡수(마이그레이션 없음)
- 테스트 3건 추가 (누적 449건 전체 통과)

**결정 로그**

- **Gemini 는 검색 결과를 구글 리다이렉트 주소로 준다.** `vertexaisearch.cloud.google.com` 이 전부라 도메인 라벨이 무의미했다. 실제 목적지는 따라가야 알 수 있어 클라이언트에서 풀 수 없다
- 해결은 **제공자가 함께 주는 `title` 을 저장**하는 것. 이를 위해 출처 형태를 객체로 바꿨고, 이미 저장된 문서는 읽기 시점에 `{ url }` 로 감싸 흡수한다
- 제목도 없고 리다이렉트 호스트이면 `출처 1`·`출처 2` 로 최소한 구분은 되게 했다
- **fallback 출처를 4개로 제한** — 참조한 링크를 항목마다 전부 붙이니 화면이 링크로 뒤덮였다

**다음 작업**

- 남은 ⚠️ 5건(#10·#11·#13·#16·#17) 판단 후 `/pr`
