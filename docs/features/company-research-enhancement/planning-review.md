# 기획 검수 — company-research-enhancement (기업 조사 페이지 고도화)

| 항목      | 값                 |
| --------- | ------------------ |
| 모드      | 기능 단위          |
| 검수일    | 2026-08-08         |
| 라운드 수 | 3 (최대 회차 도달) |
| 최대 회차 | 3 (기본값)         |
| 최종 갱신 | 2026-08-08 — 구현 1차(스키마·키 스토어·AI 호출) 진행하며 세부 정책 4건 확정 |

## 요약

- 🚫 개발 착수 불가: **0건** (초기 3건 전부 해소) / ⚠️ 반드시 확인: **8건 잔여** / 💡 참고: **4건**
- **⚠️ #25(브라우저 직접 호출 CORS)는 실측으로 해소** — `anthropic-dangerous-direct-browser-access: true` 헤더로 통과 확인. 웹 검색 도구 포함 요청도 동일.
- **세부 정책 4건 확정 (#4·#6·#7·#26)** — 채움률 분모 7개 / `hasResearch()` 기준 유지 / AI 결과 필드별 선택 / 키 없으면 버튼 비활성화 + 안내.
- 잔여 8건(#5·#10·#11·#12·#13·#14·#16·#17)은 UI 구현 단계에서 답이 필요한 것들이며 차단 요소는 아닙니다.

## 검수 입력

| 소스              | 확보 여부 | 비고                                                                          |
| ----------------- | --------- | ----------------------------------------------------------------------------- |
| plan.md           | ✅        | `docs/features/company-research-enhancement/plan.md`                           |
| Notion 기획서     | ✅        | 메모리의 제품 기획서 링크 — 3.4 자기소개서 관리 / 3.5 기업 분석 / 3.6.1 AI 정책 |
| 기존 코드(기준선) | ✅        | `entities/company`, `features/recruitment`, `functions/src/llm.ts`, `firestore.rules` |
| 기존 기능 문서    | ✅        | `docs/features/recruitment/plan.md`·`progress.md`                              |
| Figma 시안        | 미확보    | 사용자 "없음"                                                                 |
| Confluence        | 미사용    | 프로젝트 정책상 미사용                                                        |

## 라운드별 변경

- **Round 1**: 초기 13건 도출 (탐지 3종 + 기존 코드 기준선 대조)
- **Round 2**: 신규 +5건 (저장소 제약·보안/개인정보·비용 한도 관점 추가)
- **Round 3**: 신규 +3건 (기존 화면과의 일관성·중단 흐름 관점 추가) → **최대 회차 도달, 추가 라운드 미수행**
- **2026-08-08 사용자 결정 반영 (1차)**: 공고 마감일 기능 폐기 → #3(🚫) · #8(⚠️) · #21(💡) **해소(closed)**
- **2026-08-08 사용자 결정 반영 (2차)**: AI 조사는 웹 검색 근거로, 자소서 문항 메모는 3.4로 이관 → #1(🚫) · #2(🚫) · #9(⚠️) · #15(⚠️) **해소**, #14(⚠️) 축소. 웹 검색 결정에서 파생된 **신규 ⚠️ #24** 추가.

---

## 🚫 개발 착수 불가 (0건 / 초기 3건 — 전부 해소)

#### 1. ~~AI 자동 조사가 무엇을 근거로 답하는지 정의되지 않음 (특히 "최근 이슈")~~ — ✅ 해소 (2026-08-08)

> **결정: (B) 웹 검색 도구를 붙여 실제 출처를 근거로 생성.** 서버사이드 웹 검색을 `tools`에 선언하고, 각 조사 항목에 출처 URL을 함께 저장한다. plan.md 접근 방식에 반영됨. → 파생 확인 항목은 ⚠️ #24.

#### 2. ~~"자소서 문항별 활용 메모"가 기획서 3.4 "자기소개서 관리"와 중복됨~~ — ✅ 해소 (2026-08-08)

> **결정: 3.4로 미루고 연결만.** 이번 범위에서 문항 원본을 만들지 않는다. 3.4가 미구현이라 현재 연결할 대상이 없으므로, 이번 작업은 연결 지점만 열어두고 실제 연결은 3.4 이슈에서 다룬다. plan.md Out of Scope에 반영됨.

#### 3. ~~공고 마감일을 기업(Company)에 두는 것이 도메인과 맞지 않을 수 있음~~ — ✅ 해소 (2026-08-08)

> **결정**: 마감일 기능(필드·정렬·임박 알림)을 **이번 범위에서 폐기**. 소속 엔티티 결론이 난 뒤 별도 이슈로 다룬다. plan.md Out of Scope에 반영됨. 아래는 재개 시 참고용 원문.

- **분류**: 정책 모순
- **근거**: `entities/company/model/company.type.ts` 주석 — "기업 — 지원 건과 분리해 보관한다. **같은 기업에 여러 번 지원해도 기업 정보·조사 노트는 하나만 유지**된다." 반면 마감일은 **공고마다 다른 값**이고, 공고 단위 정보는 `JobApplication`(`postingTitle` 등)에 있습니다. plan.md는 마감일을 `Company` 필드로 전제하고 있습니다.
- **확인 질문**: 마감일은 (A) 기업당 1개(가장 최근 관심 공고의 마감일, 다음 채용 때 덮어씀) (B) 지원 건(공고)마다 별도 (C) 기업 조사에는 "관심 공고 마감일" 1개만 두고 실제 지원 건 마감일은 별도 — 중 무엇인가요? 기존 `postingUrl`(기업당 1개)도 같은 성격이라, 그때 어떻게 합의했는지 기준을 재사용할지도 알려주세요.
- **권장 방향(참고 — 사람 확인 필요)**: 이번 목적이 "공고 떴을 때 빠르게 대응"이므로 (A)로 단순화하는 것도 충분해 보이나, 재지원이 잦은 공기업·금융권 특성상 (B)가 맞을 수 있어 사람 판단이 필요합니다.

---

## ⚠️ 반드시 확인 (8건 잔여 / 초기 12건 + 신규 4건, 8건 해소)

#### 4. ~~AI 조사 결과와 기존 수기 입력값의 충돌 규칙 없음~~ — ✅ 해소 (2026-08-08)

> **결정: 필드별로 사용자가 선택.** 미리보기에서 기존 값과 AI 결과를 나란히 보여주고 반영할 항목만 고른다.


- **분류**: 미정의 케이스
- **근거**: plan.md "AI 결과는 즉시 저장하지 않고 사용자 확인 후 반영"까지만 있고, 이미 값이 있는 필드를 어떻게 할지는 없음.
- **확인 질문**: 이미 채워진 필드에 AI 결과가 오면 덮어쓰기 / 건너뛰기 / 필드별로 사용자가 선택 중 무엇인가요? 재실행 시(2번째 조사) 규칙도 같나요?

#### 5. AI 조사 호출 횟수·비용 한도가 정의되지 않음

- **분류**: 미정의 케이스
- **근거**: `functions/src/index.ts`의 기존 `recommendConcepts`·`generateQuestions`에 사용자별 호출 제한 로직이 보이지 않음. 기업 조사는 버튼 하나로 반복 호출이 쉬움.
- **확인 질문**: 사용자당 1일 몇 회, 기업당 몇 회로 제한할까요? 제한 없이 갈 경우 비용 상한은 누가 감당하나요? (개인 프로젝트라 무제한이어도 된다면 그렇게 명시해주세요.)

#### 6. ~~채움률(완성도) 계산 기준 미정의~~ — ✅ 해소 (2026-08-08)

> **결정: 조사 항목 7개**(관심 직무·직무 설명·자격 요건·자유 메모 + 인재상·사업 내용·최근 이슈). 기본 정보는 분모에서 제외. 가중치 없음.


- **분류**: 미정의 케이스
- **근거**: plan.md In Scope "조사 항목 채움률(완성도) 표시". 현재 `hasResearch()`는 4개 필드 중 1개라도 있으면 true인 이진 판정.
- **확인 질문**: 채움률의 분모에 어떤 필드가 들어가나요(기업명·분류 같은 기본 정보 포함 여부)? 필드별 가중치가 있나요? 자소서 문항 메모도 분모에 포함되나요?

#### 7. ~~신규 필드 추가로 기존 "조사 완료" 판정이 바뀌는 문제~~ — ✅ 해소 (2026-08-08)

> **결정: `hasResearch()`는 기존 4개 필드 기준을 유지.** 필터·카운트 결과가 안 바뀐다. 완성도는 채움률로 따로 표시.


- **분류**: 정책 모순
- **근거**: `hasResearch()`에 신규 필드를 반영하면 기준이 달라지고, 목록 상단 "조사 완료 N건"·"조사 완료만" 필터 결과가 기존 데이터에서 변합니다.
- **확인 질문**: 기존에 "조사 완료"로 보이던 기업이 신규 필드 미입력으로 "미완성"이 되어도 괜찮나요? 아니면 조사 완료 판정은 기존 4개 필드 기준을 유지하고 채움률만 신규 필드를 반영할까요?

#### 8. ~~마감일 관련 규칙 3종 미정의 (임박 기준·지난 공고·마감일 없음)~~ — ✅ 해소 (2026-08-08, 마감일 기능 폐기로 무효)

- **원문**: (a) "임박"은 며칠 전부터인가요(D-7? D-3?) (b) 마감일이 지난 기업은 숨기나요/회색 처리하나요? (c) 마감일 없는 기업의 정렬 위치는? → 마감일 기능을 다시 열 때 함께 답해야 할 질문.

#### 9. ~~자소서 문항 메모의 스코프 (기업 단위 vs 공고 단위)~~ — ✅ 해소 (2026-08-08, 3.4로 이관되어 무효)

- **원문**: 문항 메모는 기업 단위 누적인가요, 공고/회차별 분리인가요? → 3.4 자기소개서 관리 구현 시 함께 답해야 할 질문.

#### 10. AI 조사 실패 시 처리 규칙 없음

- **분류**: 흐름 누락
- **근거**: plan.md 검증 계획에 "AI 호출 실패 시 에러 처리"는 있으나 정책(재시도·부분 결과)은 미정.
- **확인 질문**: 타임아웃·쿼터 초과·JSON 파싱 실패 시 자동 재시도를 하나요(몇 회?), 아니면 에러 토스트만 띄우나요? 3개 항목 중 일부만 채워진 부분 결과도 사용자에게 보여주나요?

#### 11. 기존 `researchNote`(자유 메모)와 신규 필드의 관계

- **분류**: 미정의 케이스
- **근거**: plan.md는 기존 `researchNote` 내용을 자동 분해하지 않고 그대로 두는 것으로 초안을 잡았습니다(마이그레이션 회피). 그러면 같은 내용이 자유 메모와 신규 필드 양쪽에 남을 수 있습니다.
- **확인 질문**: 기존 자유 메모에 적힌 인재상·사업 내용을 사용자가 옮기도록 안내(배너 등)할까요, 아니면 그냥 방치할까요? 자유 메모 필드를 계속 유지하나요?

#### 12. xlsx 임포트와 신규 필드의 관계

- **분류**: 미정의 케이스
- **근거**: `xlsxParse.ts`의 헤더 매핑은 `직무`/`직무 설명`/`자격 요건`만 다룹니다. 신규 필드는 시트에 대응 열이 없습니다.
- **확인 질문**: 시트에 인재상·사업 내용 열을 추가해 임포트할 계획인가요, 아니면 신규 필드는 임포트 대상에서 완전히 제외인가요? (과거 재임포트가 조사 내용을 지운 회귀가 있었어서, 임포트가 신규 필드를 건드리지 않는다는 것을 명시해두는 편이 안전합니다.)

#### 13. 동명·계열사 기업을 AI가 어떻게 식별하는지

- **분류**: 미정의 케이스
- **근거**: 현재 `Company`의 식별 정보는 사실상 `name` 문자열뿐입니다.
- **확인 질문**: AI 조사 시 기업명만 넘기나요, 아니면 공고 URL·근무 위치 등 추가 단서를 함께 넘기나요? "삼성전자"와 "삼성SDS", 같은 이름의 중소기업이 섞이는 경우는 어떻게 처리하나요?

#### 14. 조사 텍스트 길이 상한 (축소됨 — 자소서 메모 제외로 문서 크기 위험은 해소)

- **분류**: 미정의 케이스
- **근거**: 웹 검색 근거 조사는 원문이 길어질 수 있어 AI 응답 길이 제한이 필요합니다. (자소서 메모가 빠지면서 Firestore 1MB 문서 한계 우려는 사실상 사라졌습니다.)
- **확인 질문**: 각 조사 필드의 글자 수 상한을 둘까요? "최근 이슈"는 몇 건까지 담나요?

#### 15. ~~AI에 전송되는 데이터 범위 (개인정보 관점)~~ — ✅ 해소 (2026-08-08)

> 자소서 메모가 범위에서 빠지면서 AI로 전송되는 데이터는 **기업 정보뿐**입니다. 개인정보 전송 지점이 없습니다. (향후 3.4에서 자소서 첨삭 기능이 생기면 그때 다시 판단.)

#### 16. 검색 대상에 신규 필드를 포함할지

- **분류**: 흐름 누락
- **근거**: `useCompanyResearch.ts` 검색은 `name`/`targetJob`/`jobDescription`/`requirements`만 대상입니다. 신규 필드는 자동으로 포함되지 않습니다.
- **확인 질문**: 인재상·사업 내용·최근 이슈·자소서 메모도 키워드 검색 대상에 넣을까요? (넣으면 "블록체인" 같은 키워드로 기업을 찾는 시나리오가 가능해집니다.)

#### 24. ~~웹 검색을 쓰려면 Daily 기능과 다른 모델이 필요함~~ — ✅ 해소 (2026-08-08, 호출 경로 변경으로 무효)

> FE에서 직접 호출하게 되면서 `functions/src/llm.ts`의 `MODEL` 상수와 분리됐습니다. 기업 조사는 `claude-sonnet-5`(웹 검색 지원)를 쓰고 Daily는 Haiku 4.5 그대로 둡니다. 비용도 사용자 부담이라 우리 쪽 상한 문제가 없어졌습니다.

#### 25. ~~브라우저에서 Anthropic API 직접 호출이 실제로 되는지 미검증~~ — ✅ 검증 완료 (2026-08-08)

> **결과: 성립합니다.** Chromium에서 `http://localhost` origin으로 실제 `fetch`를 실행해 확인했습니다.
>
> | 시나리오 | 결과 |
> | --- | --- |
> | 헤더 없이 | ❌ preflight 차단 — `No 'Access-Control-Allow-Origin' header` |
> | `anthropic-dangerous-direct-browser-access: true` | ✅ 통과 — HTTP 401 `invalid x-api-key` (더미 키 사용, 서버 도달 확인) |
> | 위 헤더 + `web_search_20260209` 도구 포함 | ✅ 통과 — HTTP 401 |
>
> 이 헤더가 CORS를 여는 스위치이며, 웹 검색 도구를 포함해도 브라우저 경로에서 막히지 않습니다. `curl`은 CORS를 강제하지 않으므로 검증 수단이 아닙니다.

#### 26. ~~키가 없는 사용자의 동선이 정의되지 않음~~ — ✅ 해소 (2026-08-08)

> **결정: 버튼은 보이되 비활성화 + 안내.** 키가 필요하다는 문구와 설정 링크를 함께 노출.


- **분류**: 흐름 누락
- **근거**: plan.md에 키 입력 UI는 있으나, 키 없는 상태에서 기존 화면이 어떻게 보이는지는 없습니다. 지인 소수가 쓴다면 **키를 발급받지 않은 사용자가 다수**일 수 있습니다.
- **확인 질문**: 키가 없을 때 AI 조사 버튼을 (a) 숨기나요 (b) 비활성화하고 안내 툴팁을 다나요 (c) 눌렀을 때 키 설정 화면으로 보내나요? 키 발급 방법 안내(Anthropic 콘솔 링크 등)는 어디에 두나요?

#### 27. ~~Daily 학습과 AI 호출 경로가 갈림~~ — ✅ 해소 (2026-08-08)

> **결정: (b) Daily도 같은 사용자 키 방식으로 이전.** 후속 이슈 [#82](https://github.com/qlalfdmlghk1/Template-Tester/issues/82)로 분리했습니다.
>
> - **착수 순서**: #81의 브라우저 직접 호출 검증(⚠️ #25)이 통과한 뒤에 시작합니다. 막힐 경우 돌아갈 곳을 남겨두기 위해 Functions를 먼저 지우지 않습니다.
> - **이번 이슈(#81)에 미치는 영향**: 키 스토어를 기업 조사 전용이 아니라 **공용(`shared/model`)으로 설계**합니다. #82에서 그대로 재사용합니다.
> - **감안할 점**: Daily는 AI 출제가 기능의 핵심이라 키가 없으면 사실상 사용 불가가 됩니다. 본인 + 지인 소수 사용 전제 위에서 성립하는 결정입니다.

#### 17. AI 조사 진행 중 이탈 시 동작

- **분류**: 흐름 누락
- **근거**: 조사는 수 초~수십 초 걸리는 호출인데, plan.md에는 진행 중 상태의 수명이 정의돼 있지 않습니다.
- **확인 질문**: 조사 중에 다이얼로그를 닫거나 다른 페이지로 이동하면 요청을 취소하나요, 백그라운드로 계속 두고 완료 시 알리나요? 여러 기업을 동시에 조사할 수 있나요?

---

## 💡 참고 (4건 / 초기 6건)

#### 18. AI 조사 결과의 출처 표기

- 사용자가 사실 여부를 검증할 수 있도록 "AI가 생성한 초안입니다" 배지나 조사 시각을 함께 남길지. (환각 대비)

#### 19. ~~모델 선택과 비용~~ — ⚠️ #24로 승격 (2026-08-08)

> 웹 검색 결정으로 "품질이 부족하면 검토" 수준이 아니라 **모델을 바꾸지 않으면 기능이 성립하지 않는** 문제가 되어 ⚠️로 올렸습니다.

#### 20. Functions 리전·시크릿 재사용 확인

- 신규 callable도 기존과 동일 리전·`ANTHROPIC_API_KEY` 시크릿을 쓰는지. (`src/shared/api/firebase.ts`의 `getFunctions(app)`가 기본 리전을 쓰고 있어 리전 불일치 시 호출 실패)

#### 21. ~~마감일 알림을 지원 현황 페이지에도 노출할지~~ — ✅ 해소 (2026-08-08, 마감일 기능 폐기로 무효)

#### 22. 서버 상태 관리 패턴

- `useCompanies`가 TanStack Query 미사용(수동 `reload()`)이라 AI 조사 후 갱신도 전체 재조회가 됩니다. 이번 범위에서 바꾸지 않기로 했으나, 호출이 늘어나면 체감이 나빠질 수 있습니다.

#### 23. 접근성·모바일

- 채움률 표시를 색으로만 구분하지 않기(색각 이상 고려), 모바일 폭에서 마감 배지가 카드 레이아웃을 깨지 않는지.

---

## 최종 확인 질문 목록

> 기획자/사용자에게 그대로 전달할 수 있는 질문 모음 (심각도 순).

1. ~~[🚫] AI 자동 조사는 (A) 모델 지식만 (B) 웹 검색 근거 (C) 원문 요약 중 무엇인가요?~~ → ✅ **(B) 웹 검색 근거**로 확정
2. ~~[🚫] "자소서 문항별 활용 메모"는 3.4의 선행 구현인가요, 별개 기능인가요?~~ → ✅ **3.4로 미루고 연결만**으로 확정
3. ~~[🚫] 공고 마감일은 기업당 1개인가요, 지원 건(공고)마다 별도인가요?~~ → ✅ 마감일 기능 폐기로 해소
3b. ~~[⚠️ #24] 웹 검색 지원 모델로 올릴까요?~~ → ✅ 호출 경로 변경으로 해소 (`claude-sonnet-5`, Daily와 분리)
3c. ~~[⚠️ #25] 브라우저에서 Anthropic API 직접 호출이 되나요?~~ → ✅ 2026-08-08 실측 확인 (헤더 필요, 웹 검색 포함도 통과)
3d. **[⚠️ 신규 #26] 키가 없는 사용자에게 AI 조사 버튼을 숨길까요, 비활성화할까요, 키 설정으로 유도할까요?**
3e. ~~[⚠️ #27] Daily 학습의 AI 기능은 Functions 경로 그대로 둘까요?~~ → ✅ 사용자 키 방식으로 이전, 후속 이슈 #82
4. [⚠️] 이미 값이 있는 필드에 AI 결과가 오면 덮어쓰기/건너뛰기/필드별 선택 중 무엇인가요?
5. [⚠️] AI 조사 호출 횟수 제한(사용자당·기업당)과 비용 상한은 어떻게 두나요?
6. [⚠️] 채움률의 분모에 들어가는 필드와 가중치는 무엇인가요?
7. [⚠️] 신규 필드 때문에 기존 기업이 "조사 미완성"으로 바뀌어도 되나요? "조사 완료" 판정 기준은 유지인가요 변경인가요?
8. ~~[⚠️] 마감 "임박" 기준은 며칠 전부터인가요? 지난 공고와 마감일 없는 기업은 각각 어떻게 표시·정렬하나요?~~ → ✅ 마감일 기능 폐기로 해소
9. ~~[⚠️] 자소서 문항 메모는 기업 단위 누적인가요, 공고/회차별 분리인가요?~~ → ✅ 3.4 이관으로 해소
10. [⚠️] AI 조사 실패 시 자동 재시도를 하나요? 부분 결과도 보여주나요?
11. [⚠️] 기존 자유 메모(`researchNote`)의 내용을 신규 필드로 옮기도록 안내할까요? 자유 메모는 계속 유지하나요?
12. [⚠️] 신규 필드를 xlsx 임포트 대상에 포함하나요, 완전히 제외하나요?
13. [⚠️] AI 조사 시 기업명 외에 어떤 식별 정보를 함께 넘기나요? 동명·계열사는 어떻게 구분하나요?
14. [⚠️] 조사 필드 글자 수 상한은 얼마인가요? "최근 이슈"는 몇 건까지 담나요?
15. ~~[⚠️] AI에는 기업 정보만 보내고 자소서 메모는 보내지 않는 것으로 확정할까요?~~ → ✅ 자소서 메모 제외로 해소 (기업 정보만 전송)
16. [⚠️] 신규 필드도 키워드 검색 대상에 포함하나요?
17. [⚠️] 조사 중 화면을 벗어나면 취소인가요, 백그라운드 유지인가요? 동시 조사가 가능한가요?
18. [💡] AI 생성 표시·조사 시각을 남길까요?
19. ~~[💡] 조사 품질이 부족하면 이 기능만 상위 모델을 쓸 수 있나요?~~ → ⚠️ #24(3b)로 승격
20. [💡] 신규 Function의 리전·시크릿은 기존과 동일한가요?
21. ~~[💡] 마감 임박 표시를 지원 현황 페이지에도 넣을까요?~~ → ✅ 마감일 기능 폐기로 해소
22. [💡] `useCompanies`의 수동 재조회 패턴을 이번엔 유지하는 게 맞나요?
23. [💡] 채움률을 색 외 수단으로도 구분하나요?

## 사람 결정 필요 여부

- **개발 착수 불가 0건 여부**: **예 (0건)** → 외부 리소스 생성 가능 (`/start` Stage 2~4 진행 가능)
- **사람이 반드시 결정해야 하는 정책 항목**: 3건 모두 해소
  1. ~~AI 자동 조사의 정보 출처~~ — ✅ 웹 검색 근거로 확정
  2. ~~자소서 문항 메모와 기획서 3.4의 관계~~ — ✅ 3.4로 이관, 연결만
  3. ~~공고 마감일의 소속 엔티티~~ — ✅ 이번 범위에서 폐기
- ~~**착수 즉시 확인 필요**: ⚠️ #25~~ → ✅ 2026-08-08 실측 통과
- **착수 후 곧 답이 필요한 항목**: #26(키 없는 사용자 동선), #4(AI 결과 덮어쓰기 규칙), #6·#7(채움률 기준과 기존 "조사 완료" 판정 변화)
- **후속 이슈로 분리**: #27 → [#82](https://github.com/qlalfdmlghk1/Template-Tester/issues/82) (Daily AI 이전 + Functions 제거, #81 이후 착수)
- **AI가 임의로 확정한 정책**: 없음
